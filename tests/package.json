{
  "name": "sleepsensor-tests",
  "private": true,
  "type": "module",
  "description": "Dev-only test harness for SleepSensor backend modules. Not part of the deployed site.",
  "scripts": {
    "test": "node run-all.mjs"
  },
  "devDependencies": {
    "@tensorflow/tfjs": "^4.22.0",
    "fake-indexeddb": "^6.0.0"
  }
}
