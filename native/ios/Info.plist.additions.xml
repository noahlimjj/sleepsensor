<!-- Merge into ios/App/App/Info.plist (inside the top-level <dict>) -->

<key>NSMicrophoneUsageDescription</key>
<string>SleepSensor listens through the microphone while you sleep to detect snoring and teeth grinding. All audio is processed on your device and never uploaded.</string>

<key>UIBackgroundModes</key>
<array>
    <string>audio</string>
</array>

<!-- keeps the app responsive to audio-session events on the lock screen -->
<key>UIApplicationSupportsIndirectInputEvents</key>
<true/>
